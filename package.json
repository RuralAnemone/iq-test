{
	"dependencies": {
		"express": "^4.19.2",
		"prettier": "^3.2.5",
		"sheetdb-node": "^0.0.12"
	},
	"scripts": {
		"start": "type bun &> /dev/null || curl -fsSL bun.sh/install | bash && bun run index.js",
		"format": "prettier --write ."
	}
}
